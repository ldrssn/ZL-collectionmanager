# Optional Image Upload & Placeholder

The goal is to remove the mandatory image requirement when adding items or combinations, and display a pink camera icon placeholder when no image is available.

## User Review Required
None. This is a non-breaking UI/UX enhancement.

## Proposed Changes

### Components

#### [MODIFY] [AddItemForm.tsx](file:///c:/_ldrssn/Github/ZL-collectionmanager/components/AddItemForm.tsx)
- Remove `!photo` check in `handleSubmit`.
- Ensure item creation proceeds even if `photo` is null/empty.

#### [MODIFY] [KombiBuilder.tsx](file:///c:/_ldrssn/Github/ZL-collectionmanager/components/KombiBuilder.tsx)
- The logic already uses a placeholder if no photo is provided (`getPlaceholder`).
- I will verify if `getPlaceholder` returns a valid image URL.
- I will update `Step 4` (Preview) to handle cases where items in the combination might not have images (displaying the placeholder instead).

#### [MODIFY] [ItemCard.tsx](file:///c:/_ldrssn/Github/ZL-collectionmanager/components/ItemCard.tsx)
- Update the image rendering logic.
- If `item.photo` is missing or empty, render a generic container with a pink camera SVG icon.

#### [MODIFY] [ItemListItem.tsx](file:///c:/_ldrssn/Github/ZL-collectionmanager/components/ItemListItem.tsx)
- Similar update as `ItemCard.tsx` for the list view.

#### [NEW] [CameraPlaceholder.tsx](file:///c:/_ldrssn/Github/ZL-collectionmanager/components/CameraPlaceholder.tsx)
- Reusable camera placeholder component.

---

# KombiBuilder Enhancements

Refining the combination building process to allow for more flexibility.

## Proposed Changes

### [KombiBuilder.tsx](file:///c:/_ldrssn/Github/ZL-collectionmanager/components/KombiBuilder.tsx)
- **State Update**: Add `selectedAccessoire` state.
- **Workflow update**: 
    - Increase steps from 4 to 5.
    - Step 3 (Henkel): Make optional by adding a "Skip" button.
    - Step 4 (New): Accessoire selection (Optional).
    - Step 5: Final naming and saving.
- **Save Logic**: 
    - Update `combinedColors` to include accessory colors if selected.
    - Update `notes` to include the accessory name if selected.
- **UI Update**: 
    - Update StepIndicator.
    - Update Preview grid in the final step to accommodate 4 items (Körper, Klappe, Henkel, Accessoire).

## Verification Plan

### Manual Verification
1.  Open KombiBuilder.
2.  Select Körper and Klappe.
3.  Skip Henkel selection.
4.  Optionally pick an Accessoire.
5.  Save the combination.
6.  Verify the combination details (notes, colors) are correct.
7.  Check that the preview displays correctly even if some parts are skipped.

## Verification Plan

### Manual Verification
1.  Open "Add Item" form.
2.  Fill in name and details but DO NOT upload a photo.
3.  Save the item.
4.  Verify the item appears in the grid/list with a pink camera icon.
5.  Create a Kombi using this item (if possible).
6.  Save a Kombi without uploading a dedicated photo.
7.  Verify the Kombi card shows the generated placeholder (already existing logic) or update to use the camera icon if preferred (User said "or a kombi").
